@model X.PagedList.IPagedList<K23CNT3_VuTienDuc_2310900023.Models.VtdBanner>
@using X.PagedList.Mvc.Core;

@{
    ViewData["Title"] = "Quản lý Banner";
}



<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="card card-outline card-info shadow">

            <!-- Card Header: Chứa nút Thêm mới và Ô tìm kiếm -->
            <div class="card-header">
                <h3 class="card-title">
                    <a asp-action="Create" class="btn btn-success btn-sm">
                        <i class="fas fa-plus"></i> Thêm Banner mới
                    </a>
                </h3>

                <div class="card-tools mt-1">
                    <form action="" method="get">
                        <div class="input-group input-group-sm" style="width: 250px;">
                            <input type="text" name="name" class="form-control float-right" placeholder="Nhập tên banner..." value="@ViewBag.keyword">
                            <div class="input-group-append">
                                <button type="submit" class="btn btn-default">
                                    <i class="fas fa-search"></i>
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Card Body: Chứa Bảng dữ liệu -->
            <div class="card-body table-responsive p-0">
                <table class="table table-hover table-striped text-nowrap align-middle">
                    <thead class="bg-light">
                        <tr>
                            <th class="text-center" style="width: 150px;">@Html.DisplayNameFor(model => model.FirstOrDefault().VtdImage)</th>
                            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().VtdName)</th>
                            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().VtdDescription)</th>
                            <th class="text-center">@Html.DisplayNameFor(model => model.FirstOrDefault().VtdPrioty)</th>
                            <th class="text-center">Trạng thái</th>
                            <th class="text-center" style="width: 200px;">Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="align-middle text-center p-2">
                                    <img src="@item.VtdImage" alt="Banner" class="img-thumbnail" style="width: 120px; height: 60px; object-fit: cover;" />
                                </td>
                                <td class="align-middle fw-bold">
                                    @Html.DisplayFor(modelItem => item.VtdName)
                                </td>
                                <td class="align-middle text-truncate" style="max-width: 200px;">
                                    @Html.DisplayFor(modelItem => item.VtdDescription)
                                </td>
                                <td class="align-middle text-center">
                                    <span class="badge bg-secondary">@Html.DisplayFor(modelItem => item.VtdPrioty)</span>
                                </td>
                                <td class="align-middle text-center">
                                    @if (item.VtdStatus == 1)
                                    {
                                        <span class="badge bg-success">Hiển thị</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Đang ẩn</span>
                                    }
                                </td>
                                <td class="align-middle text-center">
                                    <a asp-action="Details" asp-route-id="@item.VtdId" class="btn btn-sm btn-info" title="Chi tiết">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@item.VtdId" class="btn btn-sm btn-warning" title="Sửa">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.VtdId" class="btn btn-sm btn-danger" title="Xóa">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <!-- Card Footer: Chứa Phân trang -->
            <div class="card-footer clearfix">
                @Html.PagedListPager(Model, page => Url.Action("Index", new { page = page, name = ViewBag.keyword }), new X.PagedList.Web.Common.PagedListRenderOptions
           {
               LiElementClasses = new string[] { "page-item" },
               PageClasses = new string[] { "page-link" },
               UlElementClasses = new string[] { "pagination pagination-sm m-0 float-right" }
           })
            </div>

        </div>
    </div>
</section>